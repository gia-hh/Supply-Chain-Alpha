# Supply Chain Alpha V2 实施与提交记录

## 记录口径

本文件记录已完成的工程范围、正式运行身份和 Git 整理方式。正式实证运行发生时目录尚未初始化为 Git 仓库，因此 `reports/status/phase_*.json` 中的 `git_commit: null` 是真实记录，符合项目规格；不得事后回填。

正式运行由以下不可变标识复现：

- 源码树 SHA-256：`080b989e7848887b8e2be931774accecdf09362c6bb3fc98a15a407dc6ed003d`
- 原始数据清单 SHA-256：`58eef36092ee560e2b7b23ea3226482cfb7793e8368763ce93f8d6eab7c61132`
- 配置 SHA-256：`8eaf7161959d7c8a5391172642cb8d5b14e7cd07d751f8e5ffa19af07d1a9d7c`
- 正式终态：`PATH_B_DATA_INFEASIBILITY`
- 研究结论：`INFEASIBLE_DATA`
- 策略证据：`NOT_EVALUATED`

## 已实现范围

1. **工程契约与可复现性**：规范化配置、schema、主键和单位检查；原始资产追加写与精确清单校验；源码、配置和产物哈希；阶段状态机与 fail-closed 门禁。
2. **官方数据采集**：上交所、深交所和巨潮资讯精确 HTTPS 白名单；限速、有限重试、缓存、来源记录、重定向拒绝和完整性复核。
3. **披露抽取与实体解析**：30,126 份年报的隔离式 PDF 处理；发布日期安全化；匿名对象阻断；有效期别名、历史代码和人工覆盖审计。
4. **PIT 供应链图**：`supplier -> customer` 方向、上市区间交集、550 日最大边龄、重复证据去膨胀、来源与时点完整性检查。
5. **冻结覆盖门槛**：在接触收益、信号或持有期结果前评估真实图覆盖；覆盖不足时按规格终止为 Path B。
6. **下游工程原语**：市场面板、残差、扩散信号、统计检验、实验注册、组合成交和 P&L 均已实现，并仅通过确定性的 tiny E2E fixture 验证；未生成生产 Phase 4+ 结果。
7. **安全加固**：路径逃逸、符号链接/Windows junction、非官方下载目标、隐式重定向、异常宿主休眠计时和源码枚举性能均有防护与回归测试。

## 验证记录

- 独立验证：`PROJECT VALIDATION: PASS`
- 正式研究结论：`INFEASIBLE_DATA`
- 完整测试：442 passed，3 skipped
- 跳过项：当前 Windows 账户无创建测试符号链接的权限；不影响相应 fail-closed 非特权测试
- Ruff：通过
- 项目直接依赖：全部满足声明约束
- 静态安全扫描：未发现动态执行、`shell=True`、危险反序列化、启动持久化、凭据导出或未声明网络端点

## Git 历史整理

仓库没有可恢复的既有 Git 历史，因此没有伪造逐日开发提交。当前历史从整理时点开始：

1. `a868f5e chore: initialize repository`：建立空仓库，不含项目文件。
2. `b259e1e chore: import validated V2 Path B research snapshot`：保存与正式源码哈希一致的代码、测试、规范和 canonical 报告快照。
3. 后续独立提交仅增加事后统计诊断、经济学解释和改进路线，不改变被冻结的运行状态或结论。

原始数据约 115.66 GiB，始终不进入 Git。仓库采用显式暂存白名单和本地排除清单；正式结果保留原始数据清单及逐文件哈希作为审计依据。过期状态与 synthetic/smoke 试跑也不进入正式提交，以避免和 canonical Path B 结论混淆。

## 结论边界

本项目证明的是：在 V2 预注册口径下，公开年报构成的上市公司供应链图覆盖不足。它没有检验收益预测、IC、Sharpe、策略盈利性或因果传播。任何后续改进必须形成新的版本、规则哈希和实验注册，不能改写本次冻结结论。
